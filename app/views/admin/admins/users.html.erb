<h3>List of Users</h3>

<div class="entities-container">
  <% @users.each do |user| %>
    <div class="entity-container">
      <div class="entity-container-left">
        <div>Name: <%= user.name %></div>
        <div>Email: <%= user.email %></div>
        <div>Role: <%= user.role %></div>
      </div>
      <div class="entity-container-right">
        <%= form_with url: disable_user_admin_path, method: :patch do |form| %>
          <%= form.hidden_field :user_id, value: user.id %>
          <% if user.banned? %>
            <%= form.submit 'Enable Account' %>
          <% else %>
            <%= form.hidden_field :should_disable, value: 1 %>
            <%= form.submit 'Disable Account' %>
          <% end %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
